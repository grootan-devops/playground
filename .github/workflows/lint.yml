name: Lint · Dockerfile, YAML & Docs

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    uses: grootan-devops/github-ci-library/.github/workflows/lint.yml@dev
    secrets: inherit
