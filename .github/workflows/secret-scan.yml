name: Scan · Git Secrets

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  secret-scan:
    uses: grootan-devops/github-ci-library/.github/workflows/secret-scanning.yml@dev
    secrets: inherit
    with:
      full-history: true
